version: "3.7"
services:

  caddy:
    container_name: caddy
    image: caddy:2.8.4
    restart: always
    ports:
      # - "80:80"
      - "8443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./dist:/var/www/html/dist